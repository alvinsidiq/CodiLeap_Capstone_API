To install dependencies:
```sh
bun install
```

To run:
```sh
bun run dev
```

To run migration:
```sh
// generate migration
bun run db:generate

// run migration
bun run db:migrate
```

open http://localhost:3000
